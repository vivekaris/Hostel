<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.iotwebplanet.learn.hostel">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".FinderActivity" />
        <activity android:name=".Complaint_viewActivity" />
        <activity android:name=".Wardenl_loginActivity" />
        <activity android:name=".Warden_menuActivity" />
        <activity android:name=".All_problemActivity" />
        <activity android:name=".Problem_actionActivity"></activity>
    </application>

</manifest>